import { TransactionDTO } from './dto/transaction.dto';

/**
 * Represents a transaction in the frontend.
 * This is an alias for the backend `TransactionDTO`.
 * All properties are derived from `TransactionDTO`.
 */
export type Transaction = TransactionDTO; 